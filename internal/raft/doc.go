// Package raft defines consensus-facing contracts. The Raft algorithm is Stage 3.
// It may consume storage and state contracts, but must not import transport,
// api, or membership implementations.
package raft
