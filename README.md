# dynamic-raft-kv
A distributed key-value store built using the Raft consensus algorithm, featuring dynamic cluster membership, leader election, log replication, fault tolerance, and persistent storage.
